  <div class="fixed hidden max-sm:block bottom-[70px] right-[-20px]">
        <div id='pulse' class="flex justify-center items-center">

            <img id="buttonOpenNetwork"src="/icons/iconExclamation.svg" class="w-[30px] h-[30px]" alt="icone de abrir o menu das redes sociais" class="before:hidden after:hidden"/>
            <img id="buttonCloseNetwork" src="/icons/iconClose.svg" alt="icone de fechar o menu das redes sociais" class="hidden before:hidden after:hidden"/>
    
        </div>
        <div id="icons" class="absolute w-[54px] duration-300 rounded-full -bottom-[250px] right-[30px] h-[180px] bg-gradient-to-r from-[#65c0e7] to-[#22a5dd] flex flex-col items-center">
            <img src="/icons/iconInstagram.svg" class="w-[30px] h-[30px] mt-[10px]"  alt="icone instagram"/>
            <img src="/icons/iconFacebook.svg "class="w-[30px] h-[30px] my-[10px]" alt="icone facebook"/>
            <img src="/icons/iconTwitter.svg" class="w-[30px] h-[30px]" alt="icone twit"/>
        </div>
    </div>

    
    <script>
        const icons = document.getElementById('icons') as HTMLElement;
        const pulse = document.getElementById('pulse') as HTMLElement;
        const buttonOpenNetwork = document.getElementById('buttonOpenNetwork') as HTMLElement;
        const buttonCloseNetwork = document.getElementById('buttonCloseNetwork') as HTMLElement;
        var buttonNetwork = false
        pulse.addEventListener('click', () => {
            if(buttonNetwork === true){
                pulse.classList.remove("after:hidden")
                pulse.classList.remove("before:hidden") 
                icons.classList.add("-bottom-[250px]")
                icons.classList.remove("-bottom-[50px]")
                buttonOpenNetwork.classList.remove("hidden")
                buttonCloseNetwork.classList.add("hidden")
                buttonNetwork = false
            } else if (buttonNetwork === false){
                pulse.classList.add("after:hidden")
                pulse.classList.add("before:hidden") 
                icons.classList.remove("-bottom-[250px]")
                icons.classList.add("-bottom-[50px]")
                buttonOpenNetwork.classList.add("hidden")
                buttonCloseNetwork.classList.remove("hidden")
                buttonNetwork = true
            }
        
        });
        
        </script>



    <style>


    #pulse{

    height: 5rem;

    width: 5rem;

    background: linear-gradient(

    #65c0e7,

        #22a5dd

    );

    border-radius: 50%;

    right: 30px;

    position: absolute;

    place-items: center;

    font-size: 2rem;

    color: #fff;
    z-index: 2;
    }

    /* #pulse:before,

    #pulse:after{

    content: "";

    position: absolute;

    height: 100%;

    width: 100%;

    background-color: #65c0e7;

    border-radius: 50%;

    z-index: 1;

    opacity: 0.5;

    } */

    /* #pulse:before{

    animation: pulse 5s ease-out infinite;

    } */

    /* #pulse:after{

    animation: pulse 2s 1s ease-out infinite;

    } */

    @keyframes pulse{

    100%{

        transform: scale(2.5);

        opacity: 0;

    }

    }

    </style>